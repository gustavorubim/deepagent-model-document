# ──────────────────────────────────────────────────────────────
# MRM Deep Agent – Environment Configuration
# Copy this file to .env and fill in your values.
# ──────────────────────────────────────────────────────────────

# ── Authentication mode ──────────────────────────────────────
# api  – Google AI Studio API key (simplest, no proxy needed)
# m2m  – Machine-to-machine OAuth2 client-credentials grant
# h2m  – Human-to-machine token via local shell command
MRM_AUTH_MODE=api

# ── API key mode (MRM_AUTH_MODE=api) ─────────────────────────
GOOGLE_API_KEY=your_google_ai_studio_api_key_here

# ── Vertex AI settings (required for m2m / h2m) ─────────────
GOOGLE_VERTEXAI=false
GOOGLE_PROJECT=your-gcp-project-id
GOOGLE_LOCATION=us-central1

# ── Corporate network / TLS (optional) ──────────────────────
# HTTPS_PROXY=https://proxy.example.corp:8443
# SSL_CERT_FILE=C:/certs/corp-ca-bundle.pem

# ── Vertex AI gateway (optional) ────────────────────────────
# Custom base URL routed through a corporate API gateway.
# When set, a genai.Client with HttpOptions is built automatically.
# VERTEX_BASE_URL=https://gateway.example.corp/vertex
#
# Extra HTTP headers sent with every Gemini request (JSON dict).
# VERTEX_HEADERS={"x-custom-header": "value"}

# ── M2M OAuth2 settings (MRM_AUTH_MODE=m2m) ─────────────────
# M2M_TOKEN_URL=https://auth.example.corp/oauth2/token
# M2M_CLIENT_ID=your_m2m_client_id
# M2M_CLIENT_SECRET=your_m2m_client_secret
# M2M_SCOPE=https://www.googleapis.com/auth/cloud-platform
# M2M_AUDIENCE=
# M2M_GRANT_TYPE=client_credentials
# M2M_TOKEN_FIELD=access_token
# M2M_EXPIRES_IN_FIELD=expires_in
# M2M_AUTH_STYLE=body
# M2M_TOKEN_TIMEOUT=30

# ── H2M token settings (MRM_AUTH_MODE=h2m) ──────────────────
# Shell command that prints an access token to stdout.
# H2M_TOKEN_CMD=helix auth access-token print -a
# H2M_TOKEN_TTL=1200

# ──────────────────────────────────────────────────────────────
# Corporate H2M example (Citi R2D2 gateway + helix auth):
#
# MRM_AUTH_MODE=h2m
# GOOGLE_VERTEXAI=true
# GOOGLE_PROJECT=prj-gen-ai-9571
# GOOGLE_LOCATION=global
# SSL_CERT_FILE=./CitiInternalCAChain_PROD.pem
# H2M_TOKEN_CMD=helix auth access-token print -a
# H2M_TOKEN_TTL=1200
# VERTEX_BASE_URL=https://r2d2-c3p0-icg-msst-genaihub-178909.apps.namicg39023u.ecs.dyn.nsroot.net/vertex
# VERTEX_HEADERS={"x-r2d2-soeid": ""}
# ──────────────────────────────────────────────────────────────
